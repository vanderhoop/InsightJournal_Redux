<% if @insights[:most_common_entities].nil? %>
  <div class="span8">
    <h3>You've reached the Insights page...</h3>
      <p>This is normally where we let you in on all the cool little secrets you've been keeping even from yourself, but you have to write an entry before we can make our particular brand of magic happen!</p>
    <%= link_to "Get Journaling!", "/users/#{current_user.id}/entries/new", :class => "square-button green" %>
  </div>
<% else %>

    <div class="green-container">
      <h5>You've written <%= @insights[:sample_size] %> entries for a total of <%= @insights[:total_words] %> words</h5>
    </div>
    <h2 class='title-h2'>Across <span id="num-entries">all</span> entries<span id="filtered-language"></span>...</h2></t>
    <p>Your Average Mood Score is: <strong><span id="avg-mood"><%= @insights[:avg_mood] %></span></p></strong><br>
    <p>Words Per Entry: <strong><span id="avg-word-count"><%= @insights[:avg_word_count] %></span></p></strong><br>
    <p>Your writings suggest you're <strong><span id="tense-mode"><%= @insights[:tense_mode].downcase %></span>-oriented</p></strong><br>

    <h3>Subjects: </h3>

      <ul id="most-common-entities-ul">
        <% @insights[:most_common_entities].each do |entity| %>
          <li><h4><span class="<%= entity[:most_common_sentiment] %> capitalize"><%= entity[:subject] %></span></h4></li>
            <ul>
              <li>Association: <%= entity[:most_common_sentiment] %></li>
              <li>Appearances: <%= entity[:count_total] %></li>
            </ul>
        <% end %>
      </ul>

    <h2 class="title-h2">Filters</h2>

    <%= label_tag :time_of_day, "Entries writtin in the: " %>
    <%= select_tag :time_of_day,  options_for_select([
      ["Select time of day", "none"],
      ["Morning", "morning"],
      ["Afternoon", "afternoon"],
      ["Evening","night"]
    ]) %>

    <input href=<%= "new-insights" %> type="button" value="Filter Entries" id="filter-btn" class="square-button grey">

<% end %>
